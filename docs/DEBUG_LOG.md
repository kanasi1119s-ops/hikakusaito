# デバッグ3ラウンド記録（DEBUG_LOG.md）

実施日: 2026-09-23

## Round 1（観点: 機能・データ検証）

自動テスト全件実行（Vitestユニットテスト13件、Playwright E2E 7件〔基本フロー〕）、lint（oxlint）、型チェック（tsc）、`npm run build` を実施。比較データはスキーマ検証（zod）をテストに組み込み、出典URL・確認日の欠落があればテストが失敗する構成を確認。比較データ12件中3件（25%）について、当初とは異なる検索クエリで出典を再照合（ダブルチェック）した（詳細: `docs/DATA_SOURCES.md`）。

| # | 問題 | 再現手順 | 重大度 | 対応 | 修正コミット | 再検証結果 |
|---|---|---|---|---|---|---|
| 1 | PWAのオフラインE2Eテストが失敗する。Service Workerがページを制御する前にオフライン化してリロードすると、ブラウザのネットワークエラーになりページ自体が表示できない | `npx playwright test -g オフライン` | 中 | Service Workerの `controller` がセットされ、一度オンラインの状態でリロードしてキャッシュさせてからオフライン化するようテストを修正（`e2e/site.spec.ts`）。あわせてService Worker本体（`public/sw.js`）の挙動を確認し、fetchハンドラでキャッシュ優先＋ネットワークフォールバックが正しく動作することを確認した | 本セッション内（コミット前の作業として実施） | 修正後、オフラインテストを含む7件すべて成功 |

## Round 2（観点: 異常系・境界値・表示）

空入力、該当0件の絞り込み、上限予算に0を指定、比較選択の上限（4件）超過、壊れたバックアップJSONの読み込み、スマホ幅(375px)でのレイアウト崩れ、日本語の文字化け（U+FFFD検出）、お気に入りの永続化、共有URLからの復元、について検証するE2Eテスト8件・ユニットテスト7件（`filters.test.ts`）を追加。

| # | 問題 | 再現手順 | 重大度 | 対応 | 修正コミット | 再検証結果 |
|---|---|---|---|---|---|---|
| - | 検出なし | - | - | - | - | 追加した15件のテストはすべて初回から成功。既知課題なし |

## Round 3（観点: クリーン環境・回帰・説明書検証）

リポジトリと同内容を `/tmp` 配下の別ディレクトリにコピーし（`.git` は除去）、README記載の手順（`npm install` → `npm run check`）のみに従ってセットアップからビルドまで実行。さらにビルド成果物 (`dist/`) をPythonの `http.server`（簡易HTTPサーバー）で配信し、実際にページが開けること・`manifest.webmanifest`・`sw.js` が正しいパスで配信されることを確認。同ディレクトリで `npx playwright test` を再実行し、Round 1・2で追加した15件のE2Eテストがすべて成功することを回帰確認した。

| # | 問題 | 再現手順 | 重大度 | 対応 | 修正コミット | 再検証結果 |
|---|---|---|---|---|---|---|
| - | 検出なし | - | - | - | - | クリーン環境でのセットアップ・ビルド・簡易サーバー配信・E2E回帰、いずれも問題なし |

説明書（`docs/MANUAL.md`）のボタン名・見出し名は、実装コンポーネント（`Header.tsx` / `FilterPanel.tsx` / `ExportBar.tsx` / `HowToPage.tsx`）の実際の表示文言をそのまま転記して作成し、齟齬がないことを目視で確認した。

## 総括

- 3ラウンドを通じて検出した問題: **1件（中）**。Round 1で検出し、Round 1内で修正・再検証済み。
- Round 3完了時点で重大度「高」「中」の未解決課題は **0件**。
- 低優先度の既知課題（次サイクルへの引き継ぎ）:
  - 9プランのデータが検索エンジン経由の確認にとどまっており、公式ページへの直接アクセスによる最終確認が未実施（`docs/DATA_SOURCES.md` 参照）。
  - ChatGPT Proの料金に、情報源により$100〜$200の幅があるとの報告があり、要再確認。

以上より、MVPとして完成と判定する。

## 追記: 2026-09-26サイクル（データ修正後の回帰検証）

前回サイクル終了時点で「要再確認」だったChatGPT Pro・Google AI Ultraの価格を、WebSearchでの複数情報源突き合わせにより2段階ティア（それぞれ2プラン）へ修正した（149→151プラン、詳細は`docs/DATA_SOURCES.md`）。この変更が既存機能を壊していないかを、新規の3ラウンドではなく1回の全項目回帰として検証した（データ修正のみで新機能追加を伴わないサイクルのため）。

| 検証項目 | 結果 |
|---|---|
| `npm run lint`（oxlint） | 成功（警告・エラーなし） |
| `npm run typecheck`（tsc） | 成功 |
| `npm run test`（Vitest ユニットテスト） | 27件すべて成功（スキーマ検証テストが新規2プランのレコードも含めて通過） |
| `npm run build` | 成功 |
| `npx playwright test`（E2E） | 19件すべて成功（絞り込み・並べ替え・出典表示・CSVエクスポート・オフライン閲覧を含む） |
| 秘密情報・APIキーの混入チェック | 検出なし（`GOOGLE_API_KEY`はワークフロー内で`secrets.GOOGLE_API_KEY`経由の参照のみ） |
| `npm audit` | 0件（本番・開発依存とも） |

検出した問題: **0件**。既知の持ち越し課題は変わらず「残り147プランの一次情報での最終確認が未実施」のみ（優先度順に次サイクル以降で対応）。
